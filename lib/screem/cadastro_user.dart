import 'package:flutter/material.dart';

class CadastroUserPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Cadastro User Page'),
      ),
      body: Center(
        child: Text('This is the Cadastro User Page'),
      ),
    );
  }
}
